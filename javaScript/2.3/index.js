function circleArea (radius){
    return Math.PI*radius**2;
}
const calculatedCircleArea=circleArea(2);
console.log(`the area is ${calculatedCircleArea}`);
console.log(`the rounded area is ${calculatedCircleArea.toFixed(2)}`);

